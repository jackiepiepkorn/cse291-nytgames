# cse291-nytgames

## Setup

1. Create and activate the conda environment (or install packages with `pip install -r requirements.txt`)

```
conda env create -f environment.yml
conda activate nytgames
```